# Alveo U280 编译、加载与运行手册

---

## 1. 本机环境一览

### 1.1 硬件

| 项目 | 配置 |
|------|------|
| 服务器型号 | Inspur NF5468M5 |
| CPU | 64 核 |
| 内存 | 754 GB（编译 v++ 链接时充裕） |
| 加速卡 | **8 张** Alveo U280（HBM2 8GB，32 × 256MB HBM 通道） |
| 操作系统 | CentOS 7（kernel 3.10.0-1160.108.1.el7） |
| 默认 gcc | 4.8.5（**太旧，host 编译必须切换 devtoolset**） |

### 1.2 软件与路径

| 软件 | 版本 | 路径 |
|------|------|------|
| XRT（运行时） | 2022.1（2.13.479） | `/opt/xilinx/xrt` |
| 部署平台（Platform） | `xilinx_u280_gen3x16_xdma_1_202211_1` | `/opt/xilinx/platforms/` |
| 卡上 Shell | `xilinx_u280_gen3x16_xdma_base_1` | （已烧录，8 卡全部 Ready） |
| Vitis 统一平台 | **2023.1** | `/apps/Xilinx2023/Vitis/2023.1` |
| Vivado | 2023.1 | `/apps/Xilinx2023/Vivado/2023.1` |
| Vitis HLS | 2023.1 | `/apps/Xilinx2023/Vitis_HLS/2023.1` |
| devtoolset（编译 host 用） | 7 / 8 / 9 | `/opt/rh/devtoolset-*` |

> ⚠️ **版本说明**：编译工具链是 Vitis **2023.1**，而 XRT/平台是 **2022.1**。
> 此组合可以正常工作（高版本 v++ 可面向低版本平台编译），运行时
> 可能会打印 XRT 版本不一致的 warning，一般可忽略；如出现兼容性问题，
> 可改用 2022.1 的 v++ 重新编译。

### 1.3 板卡 BDF 列表（8 张卡）

| 序号 | BDF（用户函数） | Shell |
|------|----------------|-------|
| 0 | `0000:3d:00.1` | xilinx_u280_gen3x16_xdma_base_1 |
| 1 | `0000:3e:00.1` | 同上 |
| 2 | `0000:40:00.1` | 同上 |
| 3 | `0000:41:00.1` | 同上 |
| 4 | `0000:b1:00.1` | 同上 |
| 5 | `0000:b2:00.1` | 同上 |
| 6 | `0000:b4:00.1` | 同上 |
| 7 | `0000:b5:00.1` | 同上 |

查看卡状态：`xbutil examine`（需先 source XRT 环境，见下节）。

---

## 2. 环境配置（每次使用前必做）

### 2.1 一键环境脚本

> ✅ 本机已创建好 `~/setup_u280.sh`，直接 `source ~/setup_u280.sh` 即可。
> 内容如下（如需重建可照抄）：

```bash
#!/bin/bash
# U280 开发环境一键配置（bash，请勿用 sh 执行）

# 1. Vitis 2023.1（其 settings64.sh 会同时 source Vivado 与 Vitis_HLS）
source /apps/Xilinx2023/Vitis/2023.1/settings64.sh

# 2. XRT 2022.1 运行时
source /opt/xilinx/xrt/setup.sh

# 3. host 程序编译需要新 gcc（系统默认 4.8.5 无法编译 XRT C++14 代码）
source /opt/rh/devtoolset-9/enable

# 4. 常用环境变量
export PLATFORM=xilinx_u280_gen3x16_xdma_1_202211_1

echo "Vitis : $(which v++)"
echo "XRT   : $XILINX_XRT"
echo "gcc   : $(gcc --version | head -1)"
```

### 2.2 验证环境

```bash
v++ --version          # 应显示 v2023.1
xbutil examine         # 应列出 8 张 U280，Device Ready = Yes
echo $XILINX_XRT       # 应为 /opt/xilinx/xrt
```

---

## 3. 开发流程总览

一个 Vitis 加速应用由两部分组成：

```
+---------------------+        +----------------------+
|  Host 程序 (x86 CPU) |  <---> |  Kernel (FPGA bit流)  |
|  g++ 编译            |  XRT   |  v++ 编译成 .xclbin   |
|  调用 OpenCL/XRT API |        |  由 v++ -c 和 -l 生成 |
+---------------------+        +----------------------+
```

**三种编译目标（target）**：

| target | 用途 | 编译速度 | 运行速度 |
|--------|------|----------|----------|
| `sw_emu` | 软件仿真（CPU 模拟，调逻辑） | 秒级 | 慢 |
| `hw_emu` | 硬件仿真（RTL 级仿真，调接口/性能） | 数分钟 | 很慢 |
| `hw` | 真实比特流（上卡运行） | **数小时** | 真实速度 |

典型开发顺序：`sw_emu` 调通逻辑 → `hw_emu` 看波形/性能 → `hw` 上真卡。

---

## 4. 编译 Kernel（生成 .xo）

Kernel 源码可以是 C/C++（HLS 方式）、RTL（Verilog/VHDL）或 OpenCL C。

```bash
v++ -c -t hw \
    --platform $PLATFORM \
    -k krnl_vadd \
    -o krnl_vadd.xo \
    src/krnl_vadd.cpp
```

参数说明：

| 参数 | 含义 |
|------|------|
| `-c` | compile 模式，只编译不链接 |
| `-t hw` | target：sw_emu / hw_emu / hw |
| `--platform` | 平台名，本机固定为 `xilinx_u280_gen3x16_xdma_1_202211_1` |
| `-k` | kernel 顶层函数名 |
| `-o` | 输出 .xo（Xilinx Object）文件 |
| `--hls.clock 300000000:krnl_vadd` | （可选）指定 kernel 频率，单位 Hz |
| `-I<dir>` | 头文件路径 |
| `--kernel_frequency 300` | （可选）链接期频率约束 MHz |

常用优化 pragma（写在 kernel 源码里）：

```cpp
#pragma HLS INTERFACE m_axi port=in1 offset=slave bundle=gmem0
#pragma HLS INTERFACE s_axilite port=in1 bundle=control
```

---

## 5. 链接生成 xclbin（v++ -l）

将一个或多个 .xo 链接成 FPGA 可加载的 `.xclbin`：

```bash
v++ -l -t hw \
    --platform $PLATFORM \
    --config connectivity.cfg \
    -o krnl_vadd.xclbin \
    krnl_vadd.xo
```

### 5.1 连接配置文件（connectivity.cfg）

U280 的内存是 **HBM（32 个 256MB 通道，HBM[0]~HBM[31]）**，外加空挂的
PL DDR（bank0/bank1，容量 0，**不可用**）。必须显式把 kernel 端口映射到 HBM 通道：

```ini
[connectivity]
nk=krnl_vadd:1:krnl_vadd_1            # kernel 例化数量与实例名
sp=krnl_vadd_1.in1:HBM[0]             # 端口 in1 → HBM 通道0
sp=krnl_vadd_1.in2:HBM[1]
sp=krnl_vadd_1.out:HBM[2]
slr=krnl_vadd_1:SLR0                  # （可选）指定 SLR 布局
```

常用 `[vivado]` 高级选项（同一 cfg 文件内）：

```ini
[vivado]
prop=run.impl_1.strategy=Performance_Explore
```

### 5.2 编译

由于服务器能力有限，如有可能，建议可以在自己机器上按照环境版本进行编译。

### 5.3 编译过程检查

```bash
# 链接日志与报告（在工程目录下）
<kernel>.link_summary          # 用 vitis_analyzer 打开
_logs/                         # v++ 日志
```

```bash
vitis_analyzer krnl_vadd.link_summary   # GUI 查看资源/时序报告
```

---

## 6. 编译 Host 程序（g++ + XRT）

Host 代码使用 XRT 原生 API（`xrt::` C++ API，2022.1 推荐）或 OpenCL API。

```bash
g++ -std=c++14 -O2 -Wall \
    -I$XILINX_XRT/include \
    -L$XILINX_XRT/lib \
    -o host src/host.cpp \
    -lxrt_coreutil -lpthread -lrt -ldl
```

若使用 OpenCL API 风格：

```bash
g++ -std=c++14 -O2 \
    -I$XILINX_XRT/include \
    -L$XILINX_XRT/lib \
    -o host src/host.cpp \
    -lxilinxopencl -lpthread -lrt -ldl
```

> ⚠️ 必须先 `source /opt/rh/devtoolset-9/enable`（或 devtoolset-8），
> 系统默认 gcc 4.8.5 不支持 XRT 头文件里的 C++14 语法。

最小 host 程序（XRT 原生 API）骨架：

```cpp
#include "xrt/xrt_device.h"
#include "xrt/xrt_kernel.h"
#include "xrt/xrt_bo.h"

int main() {
    int dev_idx = 0;                                  // 第几张卡
    auto device = xrt::device(dev_idx);
    auto uuid = device.load_xclbin("krnl_vadd.xclbin");
    auto krnl = xrt::kernel(device, uuid, "krnl_vadd");

    size_t bytes = 4096 * sizeof(int);
    auto bo0 = xrt::bo(device, bytes, krnl.group_id(0));   // 自动分配到 cfg 指定的 HBM
    auto bo1 = xrt::bo(device, bytes, krnl.group_id(1));
    auto bo2 = xrt::bo(device, bytes, krnl.group_id(2));

    bo0.sync(XCL_BO_SYNC_BO_TO_DEVICE);
    bo1.sync(XCL_BO_SYNC_BO_TO_DEVICE);

    auto run = krnl(bo0, bo1, bo2, 4096);
    run.wait();

    bo2.sync(XCL_BO_SYNC_BO_FROM_DEVICE);
    // ... 校验数据
}
```

---

## 7. 仿真运行（sw_emu / hw_emu）

### 7.1 生成仿真配置（每个工程目录做一次）

```bash
emconfigutil --platform $PLATFORM
# 在 2022.1+ 也可写: emconfigutil -f $PLATFORM
# 会在当前目录生成 emconfig.json
```

### 7.2 运行

```bash
# 本机混装环境必需：指向仿真兼容层（见 2.1 节说明）
export XILINX_VITIS=~/vitis_xrt221_overlay

# 软件仿真
export XCL_EMULATION_MODE=sw_emu
./host krnl_vadd.xclbin

# 硬件仿真（RTL 级，可看波形）
export XCL_EMULATION_MODE=hw_emu
./host krnl_vadd.xclbin
```

> ✅ 本手册示例已在本机实测：sw_emu 全流程输出 `TEST PASSED`。

注意：

- 仿真用对应 `-t sw_emu` / `-t hw_emu` 编译出来的 xclbin，**不能混用 hw 的**。
- 跑完真机测试前记得 `unset XCL_EMULATION_MODE`。
- hw_emu 生成波形：`emconfigutil` 后加 `--xp param:compiler.enableWaveformDebug=true`
  编译选项，或在 cfg 中加 `[debug] profile=all`。

---

## 8. 硬件运行（上真卡）

### 8.1 前提检查

```bash
xbutil examine            # 8 张卡 Device Ready = Yes
unset XCL_EMULATION_MODE  # 确保不在仿真模式
```

### 8.2 方式一：host 程序自动加载（推荐）

上面的 XRT host 程序中 `load_xclbin()` 会自动完成加载，直接运行即可：

```bash
./host krnl_vadd.xclbin
```

### 8.3 方式二：手动加载 xclbin 到指定卡

```bash
# 加载到第 0 张卡（BDF 3d:00.1）
xbutil program -d 0000:3d:00.1 -u krnl_vadd.xclbin
```

### 8.4 多卡使用

- host 程序里 `xrt::device(0)` ~ `xrt::device(7)` 对应 `xbutil examine` 列出的顺序。
- 也可通过环境变量限制可见卡：

```bash
export XRT_VISIBLE_DEVICES=0,1     # 只让应用看到 0、1 两张卡（2022.1 支持）
```

- 8 张卡互相独立，可并行跑不同 xclbin。

### 8.5 运行 OpenCL 风格程序

OpenCL 程序需要指定设备（host 代码里选 device），或配合 `xbutil program` 预载。

---

## 9. 完整示例：vadd（可直接照抄跑通）

> ✅ 本机已在 `~/vadd_demo` 部署好该示例（含 Makefile，sw_emu 已实测通过）。
> 快速体验：`source ~/setup_u280.sh && cd ~/vadd_demo && make run_sw_emu`

目录结构：

```
vadd_demo/
├── src/
│   ├── krnl_vadd.cpp
│   └── host.cpp
├── connectivity.cfg
└── Makefile
```

`src/krnl_vadd.cpp`：

```cpp
#define DATA_SIZE 4096

extern "C" {
void krnl_vadd(const int* in1, const int* in2, int* out, int size) {
#pragma HLS INTERFACE m_axi port=in1 offset=slave bundle=gmem0
#pragma HLS INTERFACE m_axi port=in2 offset=slave bundle=gmem1
#pragma HLS INTERFACE m_axi port=out offset=slave bundle=gmem2
#pragma HLS INTERFACE s_axilite port=in1  bundle=control
#pragma HLS INTERFACE s_axilite port=in2  bundle=control
#pragma HLS INTERFACE s_axilite port=out  bundle=control
#pragma HLS INTERFACE s_axilite port=size bundle=control
#pragma HLS INTERFACE s_axilite port=return bundle=control

    for (int i = 0; i < size; i++) {
#pragma HLS PIPELINE II=1
        out[i] = in1[i] + in2[i];
    }
}
}
```

`src/host.cpp`：

```cpp
#include "xrt/xrt_device.h"
#include "xrt/xrt_kernel.h"
#include "xrt/xrt_bo.h"
#include <cstdio>
#include <cstdlib>
#include <vector>

#define DATA_SIZE 4096

int main(int argc, char** argv) {
    const char* xclbin = (argc > 1) ? argv[1] : "krnl_vadd.xclbin";

    auto device = xrt::device(0);
    auto uuid = device.load_xclbin(xclbin);
    auto krnl = xrt::kernel(device, uuid, "krnl_vadd");

    size_t bytes = DATA_SIZE * sizeof(int);
    auto bo0 = xrt::bo(device, bytes, krnl.group_id(0));
    auto bo1 = xrt::bo(device, bytes, krnl.group_id(1));
    auto bo2 = xrt::bo(device, bytes, krnl.group_id(2));

    int* a = bo0.map<int*>();
    int* b = bo1.map<int*>();
    int* c = bo2.map<int*>();
    for (int i = 0; i < DATA_SIZE; i++) { a[i] = i; b[i] = 2 * i; }

    bo0.sync(XCL_BO_SYNC_BO_TO_DEVICE);
    bo1.sync(XCL_BO_SYNC_BO_TO_DEVICE);

    auto run = krnl(bo0, bo1, bo2, DATA_SIZE);
    run.wait();

    bo2.sync(XCL_BO_SYNC_BO_FROM_DEVICE);

    for (int i = 0; i < DATA_SIZE; i++) {
        if (c[i] != a[i] + b[i]) {
            printf("ERROR at %d: %d != %d\n", i, c[i], a[i] + b[i]);
            return 1;
        }
    }
    printf("TEST PASSED\n");
    return 0;
}
```

`connectivity.cfg`：

```ini
[connectivity]
nk=krnl_vadd:1:krnl_vadd_1
sp=krnl_vadd_1.in1:HBM[0]
sp=krnl_vadd_1.in2:HBM[1]
sp=krnl_vadd_1.out:HBM[2]
```

`Makefile`：

```makefile
PLATFORM := xilinx_u280_gen3x16_xdma_1_202211_1
TARGET   := hw          # sw_emu / hw_emu / hw

XRT_INC  := $(XILINX_XRT)/include
XRT_LIB  := $(XILINX_XRT)/lib

all: host krnl_vadd.xclbin

host: src/host.cpp
	g++ -std=c++14 -O2 -I$(XRT_INC) -L$(XRT_LIB) -o $@ $< \
	    -lxrt_coreutil -lpthread -lrt -ldl

krnl_vadd.xo: src/krnl_vadd.cpp
	v++ -c -t $(TARGET) --platform $(PLATFORM) -k krnl_vadd -o $@ $<

krnl_vadd.xclbin: krnl_vadd.xo connectivity.cfg
	v++ -l -t $(TARGET) --platform $(PLATFORM) \
	    --config connectivity.cfg -o $@ krnl_vadd.xo -j 16

emu_config:
	emconfigutil --platform $(PLATFORM)

run_sw_emu:
	$(MAKE) TARGET=sw_emu
	$(MAKE) emu_config
	XILINX_VITIS=$(HOME)/vitis_xrt221_overlay XCL_EMULATION_MODE=sw_emu ./host

run_hw:
	./host krnl_vadd.xclbin

clean:
	rm -rf host *.xo *.xclbin *.log _x .run _logs *.csv *.protoinst \
	       emconfig.json .Xil *.wdb *.wcfg xrc.log
```

运行步骤：

```bash
source ~/setup_u280.sh
cd vadd_demo

# 快速验证（软件仿真，1 分钟内）
make run_sw_emu

# 上真卡（编译需数小时）
make TARGET=hw
./host krnl_vadd.xclbin      # 输出 TEST PASSED 即成功
```

---

## 10. 板卡管理与诊断

所有命令需先 `source /opt/xilinx/xrt/setup.sh`。

### 10.1 查看状态

```bash
xbutil examine                          # 总览：8 张卡
xbutil examine -d 0000:3d:00.1 -r platform   # 平台/shell/时钟/MAC
xbutil examine -d 0000:3d:00.1 -r memory     # HBM 拓扑
xbutil examine -d 0000:3d:00.1 -r thermal    # 温度
xbutil examine -d 0000:3d:00.1 -r electrical # 电压电流
```

### 10.2 板卡自检

```bash
xbutil validate -d 0000:3d:00.1         # 全项自检（DMA、带宽、HBM 等）
xbutil validate -d 0000:3d:00.1 -r bandwidth
```

### 10.3 加载/卸载

```bash
xbutil program  -d 0000:3d:00.1 -u xxx.xclbin   # 加载
xbutil reset    -d 0000:3d:00.1                 # 复位卡（异常恢复首选）
```

### 10.4 运行时监控

```bash
xbutil top -d 0000:3d:00.1              # 类似 top 的动态监控
```

---

## 11. 常见问题（FAQ）

**Q1：`xbutil: command not found`**
没 source XRT 环境：`source /opt/xilinx/xrt/setup.sh`。

**Q2：host 编译报 C++ 语法错误 / `std::unique_ptr` 找不到**
默认 gcc 4.8.5 太旧：`source /opt/rh/devtoolset-9/enable`。

**Q3：运行时报 `Failed to open device` / permission denied**
当前用户需在可访问 `/dev/dri/renderD*` 的权限内，或联系管理员配置 udev 规则；
临时方案 `sudo chmod 666 /dev/dri/renderD*`（重启失效）。

**Q4：`xclbin not compatible with the shell`**
xclbin 必须是面向 `xilinx_u280_gen3x16_xdma_1_202211_1` 平台编译的 hw 版本；
仿真 xclbin 不能在真卡加载。

**Q5：链接报内存 bank 错误（bank0/bank1 0 Byte）**
U280 该平台的 PL DDR（bank0/1）容量为 0，**端口必须映射到 HBM[n]**，
在 connectivity.cfg 里用 `sp=kernel.port:HBM[n]` 指定。

**Q6：v++ 链接时被 OOM 杀掉或极慢**
确认用 `-j 16` 之类限制并行度；避开内存高峰；hw 链接建议挂 `nohup`/`tmux`。

**Q7：卡跑挂/无响应**
```bash
xbutil reset -d 0000:3d:00.1
```
不行则联系管理员 warm reboot（`xbmgmt`）。

**Q8：XRT 2022.1 与 Vitis 2023.1 混用 warning**
编译/运行时的版本 warning 可忽略。但 **sw_emu 会直接报错**
`[SW-EM 11] Unable to launch Device process`，原因是 XRT 2022.1 找
`$XILINX_VITIS/data/emulation/unified/cpu_em/...`，而 Vitis 2023.1
已改名为 `unified/sw_emu/...`。因 `/apps/Xilinx2023` 无写权限，
已用符号链接在用户目录建好兼容层，仿真前执行：

```bash
export XILINX_VITIS=~/vitis_xrt221_overlay
```

若该目录被删，可用如下脚本重建：

```bash
VITIS=/apps/Xilinx2023/Vitis/2023.1; OV=~/vitis_xrt221_overlay
mkdir -p $OV/data/emulation/unified
for d in $VITIS/*; do n=$(basename $d); [ "$n" != data ] && ln -sfn $d $OV/$n; done
for d in $VITIS/data/*; do n=$(basename $d); [ "$n" != emulation ] && ln -sfn $d $OV/data/$n; done
for d in $VITIS/data/emulation/*; do n=$(basename $d); [ "$n" != unified ] && ln -sfn $d $OV/data/emulation/$n; done
ln -sfn $VITIS/data/emulation/unified/sw_emu $OV/data/emulation/unified/sw_emu
ln -sfn $VITIS/data/emulation/unified/hw_em  $OV/data/emulation/unified/hw_em
ln -sfn $VITIS/data/emulation/unified/sw_emu $OV/data/emulation/unified/cpu_em
```

**Q9：如何看资源占用/时序是否收敛**
```bash
vitis_analyzer krnl_vadd.link_summary
```
看 Estimate（估计资源）与 Timing（WNS 必须 ≥ 0）。

**Q10：emconfig.json 放哪**
放在 host 可执行文件的**当前运行目录**，或设
`export EMCONFIG_PATH=/path/to/dir`。

---

## 12. 速查表

| 操作 | 命令 |
|------|------|
| 配环境 | `source /apps/Xilinx2023/Vitis/2023.1/settings64.sh && source /opt/xilinx/xrt/setup.sh && source /opt/rh/devtoolset-9/enable` |
| 看卡 | `xbutil examine` |
| 编 kernel | `v++ -c -t hw --platform $PLATFORM -k <kname> -o <k>.xo <k>.cpp` |
| 链接 | `v++ -l -t hw --platform $PLATFORM --config conn.cfg -o <k>.xclbin <k>.xo` |
| 编 host | `g++ -std=c++14 -I$XILINX_XRT/include -L$XILINX_XRT/lib host.cpp -lxrt_coreutil -lpthread -lrt -ldl` |
| 仿真配置 | `emconfigutil --platform $PLATFORM` |
| 仿真运行 | `XILINX_VITIS=~/vitis_xrt221_overlay XCL_EMULATION_MODE=sw_emu ./host` |
| 手动加载 | `xbutil program -d 0000:3d:00.1 -u xxx.xclbin` |
| 真机运行 | `unset XCL_EMULATION_MODE; ./host xxx.xclbin` |
| 自检 | `xbutil validate -d 0000:3d:00.1` |
| 复位卡 | `xbutil reset -d 0000:3d:00.1` |
| 查 xclbin 内容 | `xclbinutil --info -i xxx.xclbin` |

---

## 13. 参考文档

- 本机 Xilinx 文档收藏：`~/Documents/XilinxDocs`
- UG1393 Vitis 应用加速开发流程
- UG1120 Alveo 平台与 XRT
- 在线文档：https://docs.amd.com/ （搜索 "Vitis 2023.1" / "XRT 2022.1"）
